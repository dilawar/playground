pub struct Solution;

impl Solution {
    pub fn solution(nums: Vec<i32>, target: i32) -> Vec<i32> {
        vec![]
    }
}

fn main() {
    env_logger::init();
    assert_eq!(Solution::solution(vec![2, 7, 11, 15], 9), vec![0, 1]);
}
